base: baseline_bf16.yaml
experiment_name: branch_quant_vision_input_proxy
output_tag: branch_quant_vision_input_proxy
precision: int8
quantization_backend: torchao
quantization_mode: int8_weight_only
quantization_scope: vision_input_proxy
real_quantization_required: true
notes:
  - Quantizes only x_embedder.proj.1, the sole vision-specific weighted input module.
  - The following DiT is shared by video and action tokens, so this is not a full video-backbone isolation.

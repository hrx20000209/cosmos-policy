/.cuda-name
/build

# Python cache / bytecode
__pycache__/
*.py[cod]
*$py.class

# Common local caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Local training run outputs: checkpoints (DCP shards, GBs each), downloaded
# datasets, pretrained base weights, logs, diagnostics/plots. All regenerable
# or too large for git; never commit these.
/outputs/
